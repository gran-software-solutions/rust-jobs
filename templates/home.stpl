<html>
<% include!("./head.stpl"); %>

  <body>
    <% include!("./navbar.stpl"); %>

      <h1>Rust jobs</h1>
      <div>
        <ol>
          <% for job in jobs {%>
            <li>
              <%= job.title %> <a href="/jobs/<%= job.id %>">view</a>
            </li>
            <% } %>
        </ol>
      </div>
  </body>

</html>